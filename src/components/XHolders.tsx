import React from "react";

export const XHolder1 = (props: {element: any;}) => {
    return <div>{props.element}</div>;
}

export const XHolder2 = (props: {element: any;}) => {
    return <div>{props.element}</div>;
}
